# research/ (deep-dive runs) and trade-plans/ (plans + evals) are versioned as of
# 2026-09-20: they are the record the replacement engine's ledger is seeded from
# (see findings/stock-deep-dive, DECISIONS.md D8 and D14).

# Audit deep-dives — kept local, not committed.
docs/audit/

.DS_Store
*.swp
*.swo
__pycache__/
.venv/
node_modules/

# Secrets — never commit. API keys live here (auto-sourced by phase-6/7b).
.env
.claude/settings.local.json

# Machine-local Claude runtime state — holds a sessionId/pid/process-start of
# whichever machine last held the lock, so it is never portable or shareable.
.claude/scheduled_tasks.lock
